language: ruby

rvm:
  - "2.3"
  - "2.4"
  - "2.5"

branches:
  only:
    - "master"

before_install:
  - gem update --system
  - gem install bundler -v 1.16.0

cache: bundler

sudo: false

git:
  depth: 10
